syntax = "proto3"; 

service RegisterService {
    rpc Registerate(RegisterRequest) returns (RegisterResponse);
}

message RegisterRequest {
    string login = 1;
    string password = 2;
    string userName = 3;
    string date_birth = 4;
}

message RegisterResponse {
    bool result = 1;
    string error_string = 2;
}